import '../styles//navBar.css';

import { BrowserRouter as Router, Route, Link } from 'react-router-dom'
import React, { useEffect } from "react";


function NavBar() {

    // useEffect(() => { document.body.style = location.pathname.includes('/space/') ? "background: url('./img/BackBoard.jpg')" : "background: url('./img/BackMain.gif')" }, [location]);
    
    return (
        <div>
            
        
        </div>

    );
}

export default NavBar;
